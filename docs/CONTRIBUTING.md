# Contributing Guide

- Clone the repo.
- Set up venv: `python -m venv venv && source venv/bin/activate`
- Run tests: `uv run pytest tests/ -v`
- Branching: `feature/`, `bugfix/`, `data/`
- Code style: PEP 8, type hints, Pydantic
- Submit PRs to the main branch.
